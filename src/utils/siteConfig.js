module.exports = {
  siteUrl: `https://www.yarno.com.au`, // Site domain. Do not include a trailing slash!

  postsPerPage: 12, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

  siteTitleMeta: `Yarno remote learning platform | We engage and up-skill remote employees`, // This allows an alternative site title for meta data for pages.
  siteDescriptionMeta: `Yarno transforms large blocks of training into small bites of quiz-based microlearning. Oh, and it's pretty fun too (so our customers tell us).`, // This allows an alternative site description for meta data for pages.

  shareImageWidth: 1000, // Change to the width of your default share image
  shareImageHeight: 523, // Change to the height of your default share image

  shortTitle: `Yarno`, // Used for App manifest e.g. Mobile Home Screen
  siteIcon: `favicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
  backgroundColor: `#e9e9e9`, // Used for Offline Manifest
  themeColor: `#25c1a9` // Used for Offline Manifest
};
